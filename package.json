{
  "name": "examen-datascience-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Plateforme d'examen QCM en ligne - Module Data Science - Universite Chouaib Doukali",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm install --prefix client --include=dev && npm run build --prefix client && npm install --prefix server",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server"
  }
}
